//go:build tinygo.wasm

// Syscall6 is supplied by syscall_freestanding_webassembly_llgo.go. A named
// WebAssembly target must never emit the Linux kernel trap selected by its
// layout-only frontend architecture.
