name: Windows

on:
  pull_request:
    branches: ["**"]

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  host-smoke:
    name: host smoke (windows-amd64, LLVM 19, Go 1.26.5)
    runs-on: windows-latest
    timeout-minutes: 30
    env:
      LLVM_VERSION: "19.1.7"
      MSYS2_LLVM_PACKAGE_VERSION: "19.1.7-1"
      MSYS2_LIBUV_PACKAGE_VERSION: "1.52.1-1"
      MSYS2_GC_PACKAGE_VERSION: "8.2.12-1"
      MSYS2_LIBATOMIC_OPS_PACKAGE_VERSION: "7.10.0-1"
    steps:
      - uses: actions/checkout@v7

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: "1.26.5"

      - name: Set up LLVM
        # CLANG64 supplies Windows-hosted LLVM libraries used to build llgo.
        # LLGo-generated Windows code independently selects the MSVC target ABI.
        uses: msys2/setup-msys2@v2
        with:
          msystem: CLANG64
          path-type: inherit
          update: true

      - name: Install LLVM 19
        shell: msys2 {0}
        run: |
          set -euo pipefail

          repo=https://repo.msys2.org/mingw/clang64
          prefix=mingw-w64-clang-x86_64
          packages=(
            "clang-$MSYS2_LLVM_PACKAGE_VERSION"
            "clang-libs-$MSYS2_LLVM_PACKAGE_VERSION"
            "compiler-rt-$MSYS2_LLVM_PACKAGE_VERSION"
            "llvm-$MSYS2_LLVM_PACKAGE_VERSION"
            "llvm-libs-$MSYS2_LLVM_PACKAGE_VERSION"
            "lld-$MSYS2_LLVM_PACKAGE_VERSION"
            "libc++-$MSYS2_LLVM_PACKAGE_VERSION"
            "libunwind-$MSYS2_LLVM_PACKAGE_VERSION"
            "gettext-runtime-0.22.5-2"
            "libffi-3.4.6-1"
            "libiconv-1.17-4"
            "libxml2-2.12.9-2"
            "xz-5.6.3-3"
            "zlib-1.3.1-1"
            "zstd-1.5.6-2"
            "libuv-$MSYS2_LIBUV_PACKAGE_VERSION"
            "gc-$MSYS2_GC_PACKAGE_VERSION"
            "libatomic_ops-$MSYS2_LIBATOMIC_OPS_PACKAGE_VERSION"
          )
          urls=()
          for package in "${packages[@]}"; do
            urls+=("$repo/$prefix-$package-any.pkg.tar.zst")
          done
          pacman --noconfirm -U "${urls[@]}"

          for package in clang clang-libs compiler-rt llvm llvm-libs lld libc++ libunwind; do
            installed="$(pacman -Q "$prefix-$package" | awk '{print $2}')"
            if [[ "$installed" != "$MSYS2_LLVM_PACKAGE_VERSION" ]]; then
              echo "expected $package $MSYS2_LLVM_PACKAGE_VERSION, got $installed" >&2
              exit 1
            fi
          done

          installed="$(pacman -Q "$prefix-libuv" | awk '{print $2}')"
          if [[ "$installed" != "$MSYS2_LIBUV_PACKAGE_VERSION" ]]; then
            echo "expected libuv $MSYS2_LIBUV_PACKAGE_VERSION, got $installed" >&2
            exit 1
          fi

          installed="$(pacman -Q "$prefix-gc" | awk '{print $2}')"
          if [[ "$installed" != "$MSYS2_GC_PACKAGE_VERSION" ]]; then
            echo "expected BDWGC $MSYS2_GC_PACKAGE_VERSION, got $installed" >&2
            exit 1
          fi

          installed="$(pacman -Q "$prefix-libatomic_ops" | awk '{print $2}')"
          if [[ "$installed" != "$MSYS2_LIBATOMIC_OPS_PACKAGE_VERSION" ]]; then
            echo "expected libatomic_ops $MSYS2_LIBATOMIC_OPS_PACKAGE_VERSION, got $installed" >&2
            exit 1
          fi

          # Hosted runtime timers use libuv. The archive is COFF and is
          # linked with the MSVC ABI; pkgconf supplies its installation path.
          pacman --noconfirm -S --needed "$prefix-pkgconf"
          clang --version

      - name: Build and test the Windows host compiler
        shell: msys2 {0}
        run: |
          set -euo pipefail

          export CC=clang
          export CXX=clang++
          export CGO_ENABLED=1
          export LLGO_ROOT="$PWD"
          export CGO_CFLAGS="$(llvm-config --cflags)"
          export CGO_CXXFLAGS="$(llvm-config --cxxflags)"
          export CGO_LDFLAGS="$(llvm-config --ldflags --libs all --system-libs)"

          llvm_version="$(llvm-config --version)"
          if [[ "$llvm_version" != "$LLVM_VERSION" ]]; then
            echo "expected LLVM $LLVM_VERSION, got $llvm_version" >&2
            exit 1
          fi
          build_tags=byollvm,llvm19

          go version
          llvm-config --version
          go test -tags="$build_tags" \
            ./internal/meta \
            ./internal/goarch \
            ./internal/xtool/llvm
          go test -tags="$build_tags" \
            -run '^Test(ResolveGOARCHConfig|ResolveBuildConfigInvalidGOARCHValueIsAtomic|GOARCHEnv|GOARCHConfigSeparatesCacheFingerprints)$' \
            ./internal/build
          go test -tags="$build_tags" \
            -run '^(TestAcquire(AndReleaseLock(Errors)?|LockConcurrency)|TestNativeWindows.*|TestCOFFLTOLevel|TestConfigureNativeClangRootFlags)$' \
            ./internal/crosscompile
          go test -tags="$build_tags" \
            -run '^Test(WindowsTargetTriple|ARMTargetSpec)$' \
            ./ssa

          go test -tags="$build_tags" \
            -run '^(TestTargetArchAndNewTransformerArchSelection|TestMSVC.*)$' \
            ./internal/cabi

          go build -tags="$build_tags" -o llgo-windows-smoke.exe ./cmd/llgo
          ./llgo-windows-smoke.exe version

      - name: Build and run the native Windows runtime and FFI smoke tests
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"

          $vswhere = "${env:ProgramFiles(x86)}\Microsoft Visual Studio\Installer\vswhere.exe"
          $installPath = & $vswhere -latest -products * `
            -requires Microsoft.VisualStudio.Component.VC.Tools.x86.x64 `
            -property installationPath
          if (-not $installPath) {
            throw "Visual Studio C++ tools were not found"
          }
          Import-Module "$installPath\Common7\Tools\Microsoft.VisualStudio.DevShell.dll"
          Enter-VsDevShell -VsInstallPath $installPath `
            -SkipAutomaticLocation `
            -DevCmdArguments "-arch=x64 -host_arch=x64"

          $env:Path = "C:\msys64\clang64\bin;$env:Path"
          $env:LLGO_ROOT = (Get-Location).Path
          $env:LLGO_BUILD_CACHE = "off"

          # MSYS2 packages compiler-rt as a GNU-named COFF archive. Clang's
          # MSVC driver looks for the same archive using its MSVC layout/name.
          $resourceDir = (& clang -print-resource-dir).Trim()
          $gnuBuiltins = Join-Path $resourceDir "lib\windows\libclang_rt.builtins-x86_64.a"
          if (-not (Test-Path $gnuBuiltins)) {
            throw "LLVM 19 compiler-rt archive was not found at $gnuBuiltins"
          }
          $msvcRuntimeDir = Join-Path $resourceDir "lib\x86_64-pc-windows-msvc"
          New-Item -ItemType Directory -Force $msvcRuntimeDir | Out-Null
          Copy-Item $gnuBuiltins (Join-Path $msvcRuntimeDir "clang_rt.builtins.lib")

          Push-Location runtime
          # Run the core runtime smoke with BDWGC enabled. The fixture checks
          # allocation, an explicit collection, MemStats, and a finalizer.
          ..\llgo-windows-smoke.exe build `
            -o ..\windows-runtime-smoke.exe `
            .\_test\windowsruntime
          $buildExitCode = $LASTEXITCODE
          if ($buildExitCode -eq 0) {
            ..\llgo-windows-smoke.exe build -tags=nogc `
              -o ..\windows-stdlib-smoke.exe `
              .\_test\windowsstdlib
            $buildExitCode = $LASTEXITCODE
          }
          if ($buildExitCode -eq 0) {
            ..\llgo-windows-smoke.exe build `
              -o ..\windows-ffi-smoke.exe `
              .\_test\windowsffi
            $buildExitCode = $LASTEXITCODE
          }
          Pop-Location
          if ($buildExitCode -ne 0) {
            exit $buildExitCode
          }
          .\windows-runtime-smoke.exe
          if ($LASTEXITCODE -ne 0) {
            exit $LASTEXITCODE
          }

          $env:LLGO_TEST_UNRECOVERED_FAULT = "1"
          $faultOutput = .\windows-runtime-smoke.exe 2>&1 | Out-String
          $faultExitCode = $LASTEXITCODE
          Remove-Item Env:LLGO_TEST_UNRECOVERED_FAULT
          Write-Host $faultOutput
          if ($faultExitCode -eq 0) {
            throw "unrecovered Windows fault exited successfully"
          }
          foreach ($expected in @(
            "runtime error: invalid memory address or nil pointer dereference",
            "main.windowsNilFault",
            "windowsruntime\main.go"
          )) {
            if (-not $faultOutput.Contains($expected)) {
              throw "unrecovered Windows fault output is missing '$expected'"
            }
          }

          .\windows-stdlib-smoke.exe
          if ($LASTEXITCODE -ne 0) {
            exit $LASTEXITCODE
          }
          .\windows-ffi-smoke.exe
          if ($LASTEXITCODE -ne 0) {
            exit $LASTEXITCODE
          }
